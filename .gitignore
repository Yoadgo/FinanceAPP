# macOS
.DS_Store

# Claude Code — עותקי עבודה מקומיים והגדרות מקומיות, לא שייכים לריפו
.claude/worktrees/
.claude/settings.local.json

# נתוני מבחן אמיתיים — שמות סוחרים וסכומים. הריפו ציבורי; אלה לא נכנסים אליו.
apps-script/tests/fixtures.json

# עותק גיבוי מקומי של Code.gs לפני סנכרון מהעורך — לא נכנס לריפו
*.repo-backup

# נתוני בנק אמיתיים — לא נכנסים לריפו הציבורי
apps-script/tests/bank-fixture.json

# ⛔ קבצי בנק/אשראי גולמיים — הריפו ציבורי. כל xls/xlsx בשורש הוא קובץ
# עבודה זמני (המרה, בדיקה) ומכיל נתונים אמיתיים. נחסם גורף ולא לפי שם,
# כי שם הקובץ הבא לא ידוע מראש.
/*.xlsx
/*.xls
