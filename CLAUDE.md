# FinanceAPP — הנחיות עבודה

## מה זה
אפליקציית ניהול פיננסי אישי של יועד. משתמש יחיד. היום היא מנהלת תיקי השקעות;
היעד הוא מרכז בקרה פיננסי מלא — תיקים, תזרים והוצאות אשראי, הלוואות, חסכונות ופנסיה.

**מקור האמת לאפיון:** `מסמך אפיון FinanceAPP.html` בשורש. לקרוא אותו לפני כל שינוי מהותי.
הוא מחזיק את מודל הנתונים, ההחלטות שנסגרו, **מה נדחה ולמה**, ותוכנית הפיתוח בשבעה שלבים.

## הסטאק — קשיח
- HTML / CSS / JavaScript טהורים בלבד. בלי npm, בלי Node, בלי React, בלי Tailwind,
  בלי שום דבר שדורש התקנה בטרמינל — אלא אם הוסבר, הובן ואושר במפורש.
- Google Sheets כדאטהבייס, Google Apps Script כ-Backend היחיד.
- אפס עלות. בלי שרתים, אחסון או כלים בתשלום.
- הפרדת קבצים מודולרית: קובץ למסך, קובץ לרכיב.

## מבנה ומוסכמות
- סדר הטעינה ב-`index.html` קשיח: מודולים ← `app.js` (מגדיר `Pages{}`) ← מסכים.
  כל מסך רושם את עצמו: `Pages.<id> = (() => { ... return { render }; })();`
- **עקיפת מטמון:** בכל שינוי ב-JS/CSS להעלות את `?v=` בכל התגים ב-`index.html`.
  אי-עדכון = דפדפנים מגישים קבצים ישנים. זה כבר קרה.
- כל גישה לנתונים עוברת דרך `DataService`. אף מסך לא פונה לרשת בעצמו.
- RTL: לבדוק שהסדר על המסך הוא מה שהתכוונו, לא מה שה-DOM מכתיב.

## הנתונים
גיליון Google Sheets אחד. הטאבים המרכזיים:

| טאב | תוכן |
|---|---|
| `Transactions` | כל התנועות. Date · Type · Name · Symbol · Qty · ExecutionRate · Currency · Commission · Fees · TotalFX · TotalILS · CashBalanceILS · EstimatedTax · Portfolio |
| `Protfolios` | רשימת התיקים (שם הטאב כתוב בשגיאה — כך הוא בגיליון) |
| `USD_ILS` | שער דולר-שקל. שורה אחת בלבד — השער של היום |
| `RealTimeData` | מחירים חיים לכל נייר, מנוסחאות GOOGLEFINANCE |
| `Log` | יומן ריצה של ה-Apps Script: Timestamp · RunId · Function · Event · Status · Details |
| `H_<טיקר>` | היסטוריית מחירים — טאב נפרד לכל נייר, נוצר אוטומטית |

נקודות הקצה של ה-API: `transactions`, `realtime`, `history`, `usd_ils`, `health`.

## מלכודות ידועות — לקרוא לפני שנוגעים
*רועננה 7.9.26 מול הקוד. חמישה סעיפים ישנים הוסרו — הם תוארו כשבורים והיו
מתוקנים מזמן, ואחד מהם הפיל אותי לטעות במסמך מצב שכבר פורסם.*

1. **מנוע ה-FIFO ב-`portfolioEngine.js` נכון ועדין.** ההערות בו מתעדות שלוש
   מלכודות שכבר תוקנו: מפתוח לפי סימבול בלבד שערבב תיקים, ספירה כפולה של
   פיצולים, ופוזיציות רפאים ממכירה שקדמה לקנייה. לא לגעת בלי לקרוא אותן.
2. **היסטוריית מחירים היא טאב לכל נייר.** זו הסיבה לטעינה הראשונה האיטית.
3. **תפעול Apps Script — דרך הדפדפן של יועד.** ר' סעיף הקואורדינטות למטה:
   למדוד את ה-DOM בכל פתיחה, ולאמת פריסה בקריאה חיה ולא בהודעה שבמסך.
4. **פורמט כסף חי ב-`js/ui/money.js` בלבד.** שלושה מסכים כתבו אותו לעצמם עד
   ש-`FA.money` נבנה. `FA.money.ils(null)` מחזיר מקף ולא ₪0 — תא ריק אינו אפס.
5. **שתי מילים שמורות.** ״תנועות״ שייכת למשק הבית (יומן ההשקעות הוא ״יומן״),
   ו״תזרים״ שייכת למשק הבית (תזרים התיקים הוא ״מזומן בתיק״). שני מסכים
   שתובעים אותה מילה הם שני מסכים שאיש לא מבדיל ביניהם.

### מה כבר תוקן — ואל תתאר אותו כשבור
| מה שנכתב פעם | המצב בפועל |
|---|---|
| ״אין `doPost`״ | קיים, עם `withLock_` ועם `writeId` לניסיון חוזר בטוח |
| ״אין אימות״ | מושב חתום, `AUTH_REQUIRED` + `SESSION_EPOCH` בסקריפט פרופרטיז |
| ״`USD_ILS` הוא שער נקודתי״ | טאב `USD_ILS_History` קיים |
| ״אף אחד לא קורא `Commission`״ | נקרא כ**עמודה** ב-`analytics.js` — 768 תנועות, $5,059 |
| ״`.claude/worktrees` מכיל עותקים ישנים״ | התיקייה נמחקה |

`Fees` באמת אינו נקרא — אבל העמודה **ריקה לגמרי** (אפס שורות מתוך 1,660).
התווית `TRADE_COMMISSION` ב-`Classifier` היא שריד שאף כלל לא מייצר; ניקוי,
לא באג.

## פריסת Apps Script — מלכודת מאומתת (2.9.26)
**אי אפשר להחליף את קוד ה-Apps Script אוטומטית מהדפדפן.** נוסו שלוש שיטות, כולן נכשלו
*בשקט* — התוכן משתנה בעורך, ואחרי רענון הוא חוזר לישן:
1. `monaco...pushEditOperations` + לחיצה על כפתור השמירה.
2. `editor.executeEdits` + `Cmd+S` דרך אירועי מקלדת סינתטיים.
3. אירוע `paste` סינתטי עם `DataTransfer` — **התוכן כן נכנס ל-Monaco**, אבל השמירה
   עדיין לא קולטת אותו. מצב הפרויקט של Apps Script לא נגזר ממודל Monaco.

**הדרך שעובדת:** לכתוב את הקוד ללוח (`navigator.clipboard.writeText` מתוך הדף —
דורש שהטאב יהיה במיקוד, אז ללחוץ בדף קודם), ואז **יועד** עושה ידנית בעורך:
`Cmd+A` → `Cmd+V` → `Cmd+S`. שלוש הקשות.
אחרי זה אפשר להמשיך אוטומטית: הרצת פונקציה וקריאת יומן הביצוע ודיאלוג הפריסות
כן עובדים דרך `find` + לחיצה לפי `ref`.

**תמיד לאמת שמירה ברענון** ובדיקת אורך התוכן — לא להסתמך על "הכפתור נלחץ".

## איך עובדים
- לא לרוץ קדימה. אחרי כל שינוי — לעצור ולהסביר בשפה פשוטה מה נעשה ולמה.
- לשאול ולתדרך לפני פעולות מורכבות או שינויי מבנה.
- **לבדוק, לא לנחש.** לקרוא את הקוד לפני שמניחים משהו על מצבו.
  לשאלות של "מה חי בפועל" — לוודא מול הסביבה החיה או מול יועד.
- אף פעם לא להציג נתוני דמה כאילו הם אמיתיים. באפליקציה פיננסית זו טעות חמורה:
  אם אין חיבור — מצב שגיאה מפורש, או נתוני מטמון עם חותמת תאריך.
- מפתחות וסודות לא נכנסים לריפו. הריפו ציבורי. Script Properties או טאב בגיליון.
- תמיד לתת את הצעד הבא ב-GitHub Desktop: מה לסמן, מה לכתוב בקומיט, מתי Push.
- ⛔ לא להריץ פקודות git על התיקייה המקומית — זה יוצר `.git/index.lock` ותוקע את
  GitHub Desktop. לבדיקת מצב הריפו — `git clone` נפרד.

## בסוף כל סבב
לעדכן את מסמך האפיון אם החלטה השתנתה, ולרשום **למה** הוחלט כך ואיזו מלכודת התגלתה.
הקוד מעיד על מה נבנה; הוא לא מעיד על מה נשקל ונדחה.

⚠️ **ולמחוק מלכודת שתוקנה, באותו סבב.** רשימה שמתארת מצב שאינו קיים מסוכנת
יותר מרשימה ריקה, כי סומכים עליה. ב-7.9.26 חמישה סעיפים תיארו באגים מתוקנים,
ואחד מהם הביא אותי לכתוב במסמך מצב **שפורסם** שעלות החיכוך מחושבת בחסר —
כשהיא מחושבת נכון מ-3.9. חיפוש ב-`classifier.js` בלבד לא הספיק; החישוב חי
ב-`analytics.js`. **לבדוק איפה הדבר באמת קורה, לא איפה הגיוני שיקרה.**

## אפיון — סיווג לפי שורה, תגי טיול, יצירת קטגוריה (5.9.26, אושר)

**הפיצ'ר.** מסך ההוצאות מסווג היום ברמת סוחר: קבוצה אחת → קטגוריה אחת לכל
השורות שסומנו. יועד צריך לדייק שורה-שורה (פספורטכארד = 59 שורות, כמה טיולים),
וליצור קטגוריה תוך כדי — `חו"ל` לא קיימת ואי אפשר להוסיף אותה מהממשק.

**שלוש ההחלטות שנסגרו:**

1. **טיול הוא ציר שני, לא קטגוריה** → עמודת `Tag` נפרדת בגיליון Expenses.
   *נדחה:* טיול כתת-קטגוריה (`חו"ל / יוון 08.26`) — מנפח את רשימת התת-סעיפים
   עם כל טיול ומערבב שני צירים באותו שדה, כך ש"כמה על קניות" מחייב איחוד ידני.
   *נדחה:* ישות `נסיעה` עם טווח תאריכים ושיוך אוטומטי — חזק יותר, אבל טאב חדש
   ומסך ניהול. שווה רק אם יצטברו הרבה טיולים; אפשר להוסיף מעל התג בהמשך.
2. **עריכה לפי שורה יושבת בתוך הקבוצה הפתוחה** — כל שורה מקבלת בורר משלה
   שמתחיל בערך של הקבוצה; משנים רק מה שחורג. *נדחה לגרסה הבאה:* טבלת כל
   השורות בהמתנה עם סינון ומיון — עדיפה לעבודה רצופה על מאות שורות.
3. **יצירה בזמן אמת של קטגוריה ותת-קטגוריה**, אידמפוטנטית — שם קיים לא נכפל
   ולא נדרס. *נדחה:* שינוי שם ומיזוג — מחייבים עדכון רטרואקטיבי של כל השורות
   שכבר סווגו, וזו פריסה בפני עצמה.

**מה שהשרת צריך (פריסה אחת):** עמודת `Tag`; `expenses.approve` שמקבל
`items:[{id,category,subcategory,tag}]` בנוסף לצורה הישנה; `categories.upsert`;
וניתוב אליו ב-`ingestApiWrite_`.

**פתוח:** האם ציר התג מקבל תצוגה משלו במסך "על מה הוצאנו" (סיכום לפי טיול),
או נשאר רק כשדה סינון. לא נדרש לגרסה 1.

## מצב קבצי Apps Script מול הריפו (5.9.26 — מסונכרן)

שלושת הקבצים בריפו **זהים לקוד החי**, אומת בהשוואת גיבוב על התוכן המלא:
`Code.gs` (68,886 תווים) · `ingest.gs` (34,794) · `creditParser.gs` (8,022).
הפער בן 148 השורות שהיה ב-`ingest.gs` נסגר. מכאן ואילך מותר להחליף בעורך
מ-GitHub raw — אבל **רק אחרי אימות אורך וגיבוב לפני `setValue`**, ורק אם גם
המצב הנוכחי בעורך הוא הצפוי. fetch חלקי שנשמר מרוקן קובץ בייצור.

## מלכודת: הורדות מ-script.google.com (5.9.26)

הורדת קובץ ראשונה מהעורך עוברת; מהשנייה ואילך Chrome חוסם את האתר
("הורדות אוטומטיות") **בשקט** — גם בלחיצה אמיתית, בלי הודעה בדף. הסימן:
הקובץ פשוט לא מופיע ב-Downloads. הפתרון: נעילת האתר בשורת הכתובת →
הגדרות אתר → הורדות אוטומטיות → אפשר. תפריט הקובץ בעורך **לא** כולל הורדה.


## מלכודת: בורר הגרסה בדיאלוג הפריסה (5.9.26)

שתי מלכודות נפרדות, ושתיהן נכשלות **בשקט**:
1. הבורר נעול עד שלוחצים על **העיפרון** (עריכה). בלי זה הוא פשוט לא נפתח,
   וכפתור "לפריסה" אפור.
2. גם כשהוא פתוח, לחיצה על הקואורדינטה שנראית נכונה בצילום **לא נתפסת**.
   מה שעובד: לפתוח → לשאול את ה-DOM על ה-rect של האופציה → ללחוץ על מרכזה
   המדויק → **ולאמת ב-zoom שהתווית באמת התחלפה** לפני שממשיכים.

**⚠️ `Cmd+S` דרך המקלדת לא תמיד נתפס.** ב-7.9 הוא לא שמר, והדפדפן חסם
ניווט עם ״שינויים שלא נשמרו״. מה שכן עובד: כפתור השמירה בסרגל, שנמצא
לפי `aria-label="שמירת הפרויקט ב-Drive"`. הסימן לשמירה שהצליחה: הכיתוב
״שינויים שלא נשמרו״ והנקודה הכתומה ליד שם הקובץ נעלמים.

⛔ אין להקליד בעורך כשדיאלוג פתוח. הקשה תועה נחתה פעם ב-Monaco, פיצלה
מחרוזת באמצע, נשמרה אוטומטית ושרדה רענון.

## נמסר 5.9.26 — סיווג לפי שורה, תגים, יצירת קטגוריה (גרסה 13)

נבדק חי מול השרת: `categories.upsert` יוצר (`created:1`) וחוזר על עצמו בלי
לכפול (`created:0, existed:true`); `expenses.approve` עם `items` מחזיר
`updated:2`; עמודת `Tag` נוספה כעמודה 27 בלי לגעת בשורות קיימות.
137 בדיקות אוטומטיות + 12 בדיקות התנהגות ב-Playwright.

שני באגים שהבדיקה החיה תפסה ולא היו מתגלים אחרת:
- `display:flex` גובר על `[hidden]` — פאנל יצירת הקטגוריה היה גלוי תמיד.
- קטגוריה שנוצרה מהממשק נצבעה באפור של "בהמתנה". הגוון נגזר עכשיו מהשם.

## מודל משק הבית — אושר 5.9.26

**ארבעה דליים.** כל תנועה מקבלת דלי אחד, והוא קובע דבר אחד: האם היא משנה
כמה כסף יש, או רק מזיזה אותו. `הכנסה` ו-`צריכה` משנים; `העברה` (כולל
סילוק אשראי) ו-`הון` לא. רק שני הראשונים נכנסים ל״על מה הוצאנו״.
בלי החלוקה הזו האפליקציה הייתה מדווחת ₪400K הוצאות ברבעון, כשהצריכה
בפועל מהעו״ש היא ₪2,713 — פחות מ-1% ממה שיצא.

**תדירות** (`קבוע` / `חד-פעמי`) היא שדה נפרד, ולא קטגוריה. הוא מה שמבדיל
בין הלפרין (₪1,900 קבוע, נכנס לבסיס החודשי, וחודש חסר נצבע) לבין
מנדלבוים (חד-פעמי, נספר בחודשו ולא בבסיס). עובד גם על הוצאות.

**יעדי חיסכון** הם פונקציה כללית: שם, סכום, תאריך, ותנועות משויכות —
הכול ניתן לעריכה. הבית הוא מופע אחד.

### שלוש הכרעות שמצמצמות קוד
1. **לא עוקבים אחרי מקור הכסף.** ״נכנס לבריכה ובסוף יצא״. יעד עונה על
   ״כמה שולם וכמה נשאר״, לא ״מאיפה הגיע״. ביטל שכבת ייחוס שלמה.
2. **חיסכון נמדד בשתי דרכים ואסור לחבר.** תזרים = כמה הפרשנו (מהעו״ש);
   מאזן = כמה יש (מהתיק, קיים). ספירת ההעברה לאיביאי *וגם* גידול התיק
   היא ספירה כפולה.
3. **תחזית רצה על תכנון, לא על היסטוריה.** 3 חודשים בלבד, ויוני מכיל
   ₪216,554 של הבית — ממוצע עליו חסר משמעות.

### מה נלמד מרייסאפ (5.9.26)
לאמץ: **צפוי מול בפועל** על כל כרטיס עם פס התקדמות; **״נשאר להוציא״**
במקום ״הוצאת״; פיצול **קבוע/משתנה** בהכנסות ובהוצאות כאחד; **מעטפה
שבועית** להוצאות משתנות; שורה **צפויה-שלא-הגיעה** מוצגת אפורה עם תאריך
עתידי; **מעקב לפי בחירה** — לא כל קטגוריה מקבלת תקציב.

לא לאמץ: לרייסאפ **אין דלי הון או העברה**. אצלם הלפרין נספר כהכנסה
ו״הפקדות לחיסכון״ נספרות כהוצאה. במבנה כזה ₪216,554 של הבית היו נבלעים
בהוצאות, וההפקדה לאיביאי הייתה נספרת גם כהוצאה וגם כגידול בתיק. **לאמץ
את התצוגה שלהם, לא את החשבונאות.**

### מסכים
`הוצאות` נשאר מנוע קלט — סיווג ואישור, פעם בחודש כשנכנס קובץ.
`תזרים` יהיה מסך חדש — צפוי מול בפועל, נפתח כל יום. שני קצבים שונים,
ולכן שני מסכים.

## נמסר 5.9.26 — פרסר עו״ש (`bankParser.gs`)
פרסר טהור, 35 בדיקות על הקובץ האמיתי (77 תנועות, 01.06–01.09).
מאתר כותרות לפי תוכן ולא לפי מספר שורה; סימן אחיד אחד (`amount`) במקום
שתי עמודות חובה/זכות; מונה מופעים לכפילויות בין ייצואים חופפים.

⚠️ **אין בדיקת שרשור יתרות, ובכוונה.** עמודת `יתרה` ריקה ב-76 מתוך 76
המעברים בקובץ האמיתי — בדיקה עליה הייתה נכשלת תמיד או שותקת תמיד.
בדיקת השלמות היא ההתלכדות של חיובי הכרטיסים בעו״ש עם פירוט האשראי,
ושלושת סכומי 02/07 נעולים כעוגן בבדיקות.

מלכודת שהתגלתה: `bankParser.gs` נטען לבד בבדיקות, ולכן אסור לו לקרוא
לפונקציות מ-`ingest.gs` (`isoDay_`). כל פרסר חייב להיות עצמאי.

## נמסר 5.9.26 — קליטת עו״ש (טאב `Bank`)

`detectKind_` מזהה `bank`; `ingestInbox_` קולט לטאב `Bank` עם מניעת
כפילויות זהה לאשראי (מפתח + מונה מופעים — ייצואים חופפים הם המצב הרגיל).
כל שורה מקבלת הצעת דלי, ושורה שאף כלל לא זיהה נשארת `pending` **בלי
דלי**. הטאב נפתח בכל קליטה גם כשאין קובץ עו״ש, כדי שקריאה מהאפליקציה
לא תיכשל לפני הקובץ הראשון.

**החלטה: לא לשכפל את ההתאמה בין הסילוק לפירוט.** השרת כותב רק
`SettlesCard`; חישוב settled/lump/gap נשאר ב-`reconcile` שבמנוע הלקוח,
שכבר בדוק. שתי מימושים של אותה התאמה היו סוטים זה מזה בשקט. הדרה של
שורת הסילוק מהצריכה נגזרת מהדלי לבדו ואינה צריכה את הקישור.

**מלכודת שנתפסה בבדיקות:** `detectKind_` ב-`ingest.gs` קורא ל-
`isBankSheet_` ב-`bankParser.gs`. הבדיקות טענו קובץ אחד בכל פעם, ולכן
היו מחזירות `unknown` בשקט. נוסף `loadAll` ל-`tests/load.js` שטוען כמה
קבצים לסקופ אחד — כמו Apps Script — ובדיקת תלויות שמוודאת שכל פונקציה
שהענף החדש קורא לה קיימת כששלושת הקבצים יחד.

תוצאת המיון על הקובץ האמיתי (77 תנועות): הכנסה 21 · הון 26 · העברה 17 ·
צריכה 7 · **ללא דלי 6** — חמש העברות הבית ושורת מס של ₪2.

**תיקון:** `תיקונים ישראכרט - 5701` אינו ₪0 אלא **זיכוי של ₪135**
שנכנס לחשבון ב-03.08. הוא צריכה (מקטין הוצאה) ולא סילוק, ואין לו
`SettlesCard` — אחרת הוא היה מקלקל את ההתלכדות המדויקת מול פירוט
האשראי. הבדיקה תפסה זאת: הסכום יצא 75,131 במקום 74,996.

## מלכודת: מפתח לפי מיקום ברשימה שנבנית מחדש (5.9.26)

באג שיועד נתקל בו בייצור: אחרי אישור מוצלח במסך ההוצאות, כפתור של
קבוצה **אחרת** ננעל על ״שומר…״ ולא ניתן היה ללחוץ עליו יותר.

הסיבה: `_busy` היה ממופתח לפי `data-k` — שהוא **המיקום ברשימה**
(`g0`, `s3`). אחרי אישור הרשימה נבנית מחדש, הקבוצה שאושרה נעלמת,
ו-`g0` מצביע פתאום על קבוצה אחרת שיורשת את הסימון. בנוסף, המסלול
המוצלח לא ניקה את הסימון בכלל — רק ה-`catch` ניקה.

**הכלל:** מצב שנשמר על פני בנייה מחדש של רשימה חייב להיות ממופתח לפי
**תכונה של הפריט** (טוקן, שם מנורמל), לא לפי מיקומו. `data-k` נשאר
לאיתור בתוך ציור אחד; `_busyKey(item)` הוא המפתח למצב.

הבדיקה החיה שנוספה מריצה שני אישורים ברצף ומוודאת שאף כפתור לא נשאר
על ״שומר…״. אומת שהיא **נכשלת** על הקוד השבור לפני שהוסמכה כירוקה.

## נמסר 7.9.26 — עריכה אחורה, איחוד קטגוריות, שמירת מצב תצוגה

שלוש בקשות של יועד מאותה הודעה, ושלושתן נובעות מאותה הנחה שהייתה
שגויה: שסיווג הוא פעולה חד-כיוונית שקורית פעם אחת.

**1. `data-k` הפך למפתח יציב.** היה `g0`/`s3` — מיקום ברשימה. עכשיו
`g:<token>` / `s:<norm>`, אותו מפתח בדיוק כמו `_busy`. בלי זה אי-אפשר
לשחזר מצב תצוגה על פני ציור מחדש, כי כל שחזור לפי מיקום היה מחזיר
פתיחה וסימון לכרטיס **אחר**. `_itemOf` מחפש לפי מפתח ולא לפי אינדקס.

**2. שמירה לא מאפסת את המסך.** `_snap()` לפני הציור מחדש, `_restore()`
אחרי `_wire()`: כרטיסים פתוחים, פירוט שורות פתוח, בוררים ותגים שנגעו
בהם, מפת הבחירה (רק לשורות שעדיין ממתינות) ומקום הגלילה.
*הגלילה חיה ב-`#content`, לא ב-`window`* — `#app` הוא flex בגובה 100vh
עם overflow hidden. חייבים לקרוא `scrollHeight` לפני ההשמה כדי לאלץ
פריסה, אחרת הדפדפן קוצץ לגובה הישן, ועוד השמה ב-rAF לביטחון.
הצפי הנכון בבדיקה הוא ״נשמר **עד קצה התוכן**״ — הכרטיס שאושר נעלם,
התוכן קצר יותר, וקיצוץ ל-max הוא התנהגות נכונה. הכישלון שנמנע הוא
קפיצה ל-0.

**3. עריכה אחורה** — במסך ״על מה הוצאנו״, לחיצה על שורת קטגוריה פותחת
את השורות שמרכיבות אותה, כל אחת עם בורר קטגוריה, תת-קטגוריה ותג.
נשלחות **רק שורות שהשתנו** מול מצב הפתיחה, ו**בלי `rule`** — תיקון
טעות אינו הצהרה על העתיד. הכניסה דרך המספר שנראה שגוי, לא דרך תפריט.
`ExpensesEngine.rowsIn` הוא אותו סינון של `summarize` המוחזר כשורות;
בדיקה נועלת שסכום הרשימה שווה למספר שמעליה לכל קטגוריה ולכל חודש.

**4. איחוד קטגוריות** — `categories.rename` בשרת. אין פעולת ״מזג״
נפרדת: **שם חדש שכבר קיים הוא האיחוד**. הפעולה כותבת ל-`Expenses`,
ל-`Bank` ול-`Rules` (בלי הכללים, הקליטה הבאה הייתה מחזירה את השם הישן
לחיים), ומתקנת את הקטלוג — כולל מחיקת הכפילות שנוצרת, **מלמטה למעלה**.
תת-קטגוריה ריקה במקור = הקטגוריה כולה. `renamePlan_` טהור ונבדק בנפרד;
מקור ריק מחזיר מערך ריק, אחרת הוא היה תופס את כל השורות שלא סווגו.

זו הפעולה **היחידה** שנוגעת בשורות שכבר אושרו — ולכן הממשק מציג כמה
שורות יזוזו לפני האישור, ואין לה ביטול.

`?v=25`. 237 בדיקות יחידה + הרנס החי; שחזור המצב אומת **אדום** על קוד
בלי `_restore` (6 כשלים) לפני שהוסמך כירוק.

## מלכודת: קואורדינטות בעורך האפסקריפט (7.9.26 — מאומת)

הפריסה נכשלה בשקט שלוש פעמים ברצף. שלוש סיבות נפרדות, וכולן אותו שורש:

**1. צילום המסך אינו במידות ה-DOM — ורוחב הצילום עצמו אינו קבוע.**
ב-7.9 בבוקר התצוגה הייתה 1920×1117 והצילום 1444×840; באותו ערב, אחרי
שיועד הזיז את החלון, התצוגה 2560×1266 והצילום 1568×775. **קבוע 1444
מקודד בקוד יפספס.** לעבוד ביחסים ולא בפיקסלים:

```js
const r = el.getBoundingClientRect();
const fx = (r.left + r.width/2) / innerWidth;      // 0..1
const fy = (r.top  + r.height/2) / innerHeight;
// ואז: click(Math.round(fx * shotW), Math.round(fy * shotH))
// כש-shotW/shotH מגיעים מהצילום האחרון שנלקח באמת.
```

**ועדיף מזה: `find` + לחיצה לפי `ref`.** ב-7.9 בערב זה עבד על כל
כפתורי הדיאלוג (לפריסה, ניהול הפריסות, העיפרון, בורר הגרסה) ולא נדרש
חישוב בכלל. רק פריט ה-`li` ״גרסה חדשה״ עדיין חייב לחיצה במיקום.

**2. מיקומי הדיאלוג אינם קבועים בין פתיחות.** העיפרון היה פעם ב-(490,256)
ופעם ב-(537,288); כפתור ״לפריסה״ ב-(484,623) ואחר כך ב-(532,582). לחיצה
לפי מיקום שנמדד בפתיחה קודמת פוגעת ברקע, סוגרת את הדיאלוג — ולא
מדווחת דבר. **למדוד מחדש בכל פתיחה.**

**3. המקלדת לא מנווטת בבורר הגרסה.** `Up`, `Home` ו-`Enter` אינם מזיזים
את `aria-selected` ב-MDC select הזה; `Enter` פשוט מאשר את מה שכבר מסומן.
גם `el.click()` ו-dispatch ידני של pointerdown/up אינם עובדים. רק לחיצה
אמיתית במרכז ה-`li` שנמדד עכשיו.

**התוצאה של אי-הקפדה:** הדיאלוג מציג ״הפריסה עודכנה.״ עם **מספר הגרסה
הישן**. ההודעה אמיתית — הפריסה אכן עודכנה, לאותה גרסה. לכן האימות היחיד
שנחשב הוא **קריאה חיה לפעולה החדשה**, לא ההודעה במסך:

```js
await DataService.post('categories.rename',
  { category:'מזון', subcategory:'מסעדה', toCategory:'מזון', toSubcategory:'מסעדה' });
// מקור == יעד → { unchanged:true } ויוצא לפני כל כתיבה. בדיקה בטוחה.
```

## מלכודת: שם הפרויקט באפסקריפט מטעה (7.9.26)

פרויקט האפסקריפט של FinanceAPP נקרא **״בניית גליון היסטוריה למניה״** —
שם שנשאר מפרויקט המניות שממנו הועתק, ו-`Code.gs` עדיין נפתח בכותרת
`Stock DB Builder`. הוא **אינו** מופיע ברשימת הפרויקטים של
script.google.com תחת שם שקשור לפיננסים, וחיפוש ״FinanceAPP״ בדרייב
מחזיר כלום. הזיהוי הוודאי הוא לפי **חתימות הקבצים**:

| קובץ | תווים | h32 |
|---|---|---|
| `קוד.gs` | 68,886 | `1704bxd` |
| `Ingest.gs` | 47,525 | `nzp3a2` |
| `CreditParser.gs` | 8,022 | `1lru49u` |
| `BankParser.gs` | 5,741 | `15zgyc6` |

`h32` בדפדפן חייב לרוץ על **בייטים של UTF-8**, אחרת הוא לא יסכים עם
הגרסה בפייתון:

```js
const b = new TextEncoder().encode(s); let h = 2166136261 >>> 0;
for (let i = 0; i < b.length; i++) { h ^= b[i]; h = Math.imul(h, 16777619) >>> 0; }
return h.toString(36);
```

## מלכודת: raw.githubusercontent מפגר אחרי פוש (7.9.26)

ה-CDN של `raw.githubusercontent.com` החזיק תוכן ישן דקות אחרי הפוש.
**ה-API הוא מקור האמת** ואינו נשמר במטמון:

```
https://api.github.com/repos/<owner>/<repo>/contents/<path>?ref=main
```
מחזיר base64; לפענח דרך `TextDecoder` על `Uint8Array` (לא `atob` לבד —
הוא שובר עברית). לבדוק תמיד אורך **וגם** h32 לפני `setValue`.

והמלכודת שמתחתיה: ב-GitHub Desktop **קומיט ופוש הם שני כפתורים**.
`.git/refs/heads/main` מול `.git/refs/remotes/origin/main` מראה מיד אם
הפוש באמת יצא — קריאת הקבצים האלה אינה יוצרת `index.lock`.

## נמסר 7.9.26 — גרסאות 14 ו-15

**14** — `BankParser.gs` נוצר בעורך ומולא, `Ingest.gs` עודכן לגרסת העו״ש.
אומת חי: `resource=bank` מחזיר את טאב `Bank` עם הכותרות הנכונות.

**15** — `Ingest.gs` המלא כולל `categories.rename`. אומת חי: קריאת no-op
מחזירה `{moved:0, unchanged:true}`, ומקור ריק נופל על ״חסרה קטגוריית מקור״.

## מלכודת: Drive לא ממיר כל xlsx (7.9.26 — מאומת)

`driveSheetValues_` ממיר קובץ בקליטה ל-Google Sheet דרך `files.copy` עם
`mimeType: GOOGLE_SHEETS`. קובץ העו״ש של הבנק (מיוצר ב-EPPlus, עם
`EPPlusLicense.txt` בתוך ה-ZIP ושדות extra חריגים) הוחזר עם **400 Bad
Request**. אותם קבצי `.xls` של האשראי מומרים בלי בעיה — זו תכונה של
הקובץ הספציפי, לא של הקוד.

**התיקון:** לשכתב את הקובץ נקי לפני ההעלאה, בלי לגעת בערכים:

```python
ws  = openpyxl.load_workbook(src, data_only=True).active
out = Workbook(); o = out.active; o.title = ws.title
for i, row in enumerate(ws.iter_rows(), 1):
    for j, c in enumerate(row, 1):
        if c.value is not None: o.cell(row=i, column=j, value=c.value)
```
⚠️ `o.append(...)` **לא** מתאים: שורה ריקה לגמרי בראש הקובץ אינה נספרת
ב-`max_row`, וכל הגיליון זז שורה אחת. כתיבה לפי קואורדינטות שומרת על
המיקומים המקוריים.

## נמסר 7.9.26 — קליטת העו״ש רצה בהצלחה

77 תנועות נכתבו לטאב `Bank` דרך `ingest.run` (הרצה יבשה קודם, ואז
אמיתית). הפילוח בפועל, זהה למה שהבדיקות המקומיות חזו:

| דלי | שורות |
|---|---|
| הון | 26 |
| הכנסה | 21 |
| העברה | 17 |
| צריכה | 7 |
| **ללא דלי** | **6** |

₪256,608 נכנסו · ₪317,546 יצאו. ששת חסרי-הדלי הם חמש ההעברות לבית
(אסמכתא 99400) ושורת מס של ₪2 — בדיוק אלה שאין להם כלל, ושיועד יסווג.

הרצה חוזרת מחזירה `added: 0` — הדדופ לפי hash הקובץ עובד.

**מה שעדיין חסר:** אין מסך שמציג את `Bank`. הנתונים בגיליון, המסך לא נבנה.

## נמסר 7.9.26 — טאב עו״ש (גרסה 16)

המסך ״הוצאות״ נקרא עכשיו **״תנועות״** ומחזיק שלושה טאבים:
`לסיווג` · `על מה הוצאנו` · `עו״ש`. השם הישן כבר לא היה מדויק — חלק
מתנועות העו״ש הן הכנסות.

**למה טאב ולא מסך נפרד.** זו אותה פעולה בדיוק — אישור שורה — ולכן אותו
מסך, אותה חוויה, ובלי שכפול קוד. מה שכן שונה הוא **סדר השאלות**: באשראי
שואלים ״על מה״, בעו״ש שואלים קודם ״האם זה בכלל כסף שנעלם״. לכן בשורת
עו״ש הדלי הוא השדה הראשון והיחיד שחובה; קטגוריה ותדירות אופציונליות.

**המסך אינו מחשב דבר.** כל מספר מגיע מ-`BankEngine`, ואותו `rowsIn`
משרת גם את הסיכום וגם את הרשימה שנפתחת — כדי שהמספר למעלה והשורות
מתחתיו לא ייפרדו. אותה הכרעה כמו ב-`ExpensesEngine.rowsIn`.

**קיזוז מול האשראי** מוצג ולא מוסתר: שלוש מצבים — `תואם ✓`, `פער`,
`אין פירוט`. הפאנל לא משנה שום סכום (השורות ממילא בדלי `העברה`), הוא
רק מראה שהחיוב בבנק מתלכד עם הפירוט. פירוט האשראי נלקח מ-
`Pages.expenses.rows()` ולא בקריאה נוספת לשרת.

### מלכודת: `getMonth()` המקומי הוא באג שמחכה למטוס
הגיליון מחזיר `2026-05-31T21:00:00.000Z` — חצות 1.6 **בישראל**.
`x.getMonth()` נותן את התשובה הנכונה רק אם הדפדפן במקרה בישראל: אותו
קובץ נתונים היה מציג חודשים שונים ליועד ולבדיקה שרצה ב-UTC, בשקט
ובלי שגיאה. `monthKey` עובר עכשיו דרך `Intl.DateTimeFormat` עם
`timeZone: 'Asia/Jerusalem'` מפורש — אזור הזמן של **הנתונים** קבוע,
ולכן הוא נכתב ולא מונח. חמש בדיקות נועלות את זה.

### מלכודת: ערך ריק אינו "בלי סינון"
`rowsIn(rows, { bucket: '' })` אמור להחזיר את השורות **בלי דלי**. הבדיקה
`if (opts.bucket && ...)` דילגה על הסינון כי `''` הוא falsy — והמסך הראה
13 שורות תחת ״בהמתנה״ במקום 3. התנאי הוא `!== undefined`. הכלל: כשערך
ריק הוא ערך אמיתי בדומיין, היעדר הפרמטר חייב להיות `undefined`.

`?v=26`. 290 בדיקות יחידה + 46 בדיקות חיות חדשות בהרנס.

## מלכודת: לשונית עורך האפסקריפט מפסיקה למשוך מהרשת (7.9.26 — חולף)

עד היום הדרך להחליף קובץ הייתה `fetch` מ-GitHub **בתוך** לשונית העורך
ואז `setValue` על מודל Monaco. באמצע העבודה זה הפסיק לעבוד: כל `fetch`
מ-`script.google.com` — גם ל-raw וגם ל-API — **נתקע לנצח**. לא שגיאה,
לא דחייה, לא timeout. אפילו `AbortController` ל-12 שניות לא החזיר, כי
מה שנתקע הוא ה-evaluate עצמו.

**איך מזהים:** קריאה סינכרונית (`monaco.editor.getModels().length`)
עונה מיד, כלומר הלשונית חיה; רק בקשות רשת נתקעות. אותה בקשה בדיוק
מלשונית האפליקציה (`yoadgo.github.io`) חוזרת תוך שנייה.

**עקיפה שנבדקה חלקית:** להריץ את ה-`fetch` בלי `await` (fire-and-forget),
לשמור ב-`window`, ולבדוק במשיכה נפרדת — כך ה-evaluate לא נתקע. אבל
הבקשה עצמה עדיין לא מסתיימת, אז זה רק מונע את הקריסה ולא פותר.

**מה שכן זמין:** `navigator.clipboard.writeText` מלשונית האפליקציה עובד
(`document.hasFocus()` שם `true`). `readText` בלשונית העורך נכשל ב-
`NotAllowedError` כל עוד הלשונית אינה הפעילה — צריך למקד אותה קודם.
הנתיב המלא לא הושלם, ולכן **הוא לא מאומת**.

**הנתיב הבטוח שתמיד עובד:** יועד ידנית — `Cmd+A` → `Cmd+V` → `Cmd+S`
בעורך, אחרי שהקוד על הלוח. ואחריו תמיד רענון ואימות אורך + h32.

**עדכון מאוחר יותר באותו יום: זה חלף מעצמו.** ההתנתקות הייתה במקביל
לנפילת החיבור למחשב כולו, וכשהוא חזר ה-`fetch` מהעורך עבד שוב מיד.
כלומר זו לא מגבלה קבועה של `script.google.com` אלא תופעת לוואי של
נפילת הרשת בצד המחשב. **הבדיקה לפני שמסיקים:** `fetch` ל-README
מהעורך — אם הוא חוזר, הנתיב הרגיל (fetch + `setValue`) פתוח.

## נמסר 7.9.26 — מסך תזרים, חצי ״בפועל״ (`?v=27`)

עמוד הרביעי במודל — **כמה אנחנו חוסכים** — עונה על השאלה בשורה אחת:
`הכנסות − (צריכה מהעו״ש + צריכה באשראי)`. שני החצאים כבר היו מחושבים
ופשוט ישבו בשני מסכים בלי שאיש יחבר אותם.

**נבנה כ״תזרים״ ולא כ״חיסכון״**, בכוונה: זה בדיוק חצי ה״בפועל״, וכשתיכנס
תוכנית חודשית יתווסף ״צפוי״ באותו מקום. מסך ״חיסכון״ נפרד היה נזרק בסבב
הבא, ובנוסף היה מתנגש עם מסך `savings` הקיים (חסכונות = השקעות).

**`FlowEngine` אינו מחשב מחדש מה נחשב הוצאה.** הוא קורא ל-
`ExpensesEngine.summarize` ול-`BankEngine.summarize` ומחבר. כל חישוב שני
של ״מה יוצא מהצריכה״ היה סוטה מהם בשקט ביום שבו אחד מהם ישתנה.

### שלוש הכרעות שקובעות אם המספר נכון
1. **בסיס חיוב, לא תאריך עסקה.** השאלה היא כמה נשאר בחשבון בסוף החודש,
   והכסף עוזב בחיוב.
2. **שורת אשראי בלי קטגוריה כן נספרת כהוצאה** (`consume + pending`) —
   הכסף יצא, רק לא יודעים על מה. **שורת עו״ש בלי דלי לא נספרת בכלל** —
   שם באמת לא ידוע אם זו הוצאה, העברה או הון. ₪150K של העברות הבית היו
   נראים כבזבוז. היא מוצגת בפס אזהרה צהוב עם קישור לסיווג.
3. **הממוצע רץ רק על חודשים שיש בהם הכנסה.** חודש שנקלט חלקית היה מושך
   אותו למטה ומציג ״רגילות״ שמעולם לא קרתה. הפאנל אומר על כמה הוא מבוסס.

**סילוק האשראי אינו נספר פעמיים** ואין לכך קוד מיוחד: בעו״ש הוא בדלי
`העברה`, ולכן מחוץ לצריכה מעצם המודל. בדיקה נועלת שהיצא קטן מסכום
הסילוק+ההשקעות.

**שינויי ניווט:** `הכנסות והוצאות` (תזרים של תיקי ההשקעות) שונה ל-
**״תזרים תיקים״**, והטאב התחתון במובייל שנקרא ״תזרים״ מצביע עכשיו על
המסך החדש ולא על תזרים התיקים. שני מסכים תבעו את אותן מילים.

### מלכודת בבדיקה: חילוץ ספרות הופך חיסור לחיבור
`signed()` מדפיס מינוס טיפוגרפי (U+2212). בדיקה שחילצה `[^\d]` הפכה
חודש שלילי לחיובי ואישרה סכום שגוי. כל בדיקה שמסכמת כסף מהמסך חייבת
לכבד את הסימן.

37 בדיקות ל-`FlowEngine` + 24 בדיקות חיות. סה״כ 333 בדיקות יחידה.

## נמסר 7.9.26 — `money.js` וסידור הניווט (`?v=28`)

שני הפריטים הראשונים בסדר שאושר, שניהם צד לקוח בלבד.

**`js/ui/money.js`** — פורמט כסף אחד. שלושה מסכים (הוצאות, עו״ש, תזרים)
כתבו כל אחד את אותה שורה בדיוק, וזה מה ששלב 1 באפיון ביקש למנוע.
שתי משפחות: `ils`/`signed`/`delta` למשק הבית — תמיד שקלים, תמיד מעוגל,
כי אגורות בתוך עשרות אלפים הן רעש; ו-`disp`/`dispSigned` להשקעות —
מקבלות שער ומטבע **מבחוץ**, כדי שהפורמט יישאר טהור ובר-בדיקה.

⚠️ **`Number(null)` הוא 0.** בלי בדיקה מפורשת תא ריק היה מוצג כ-₪0 —
״בדקנו ואין כסף״ במקום ״אין נתון״. באפליקציה פיננסית זה בדיוק סוג
השקר שאסור. `FA.money.ils(null)` מחזיר מקף.

44 בדיקות, והאחרונות שבהן משוות את הפלט **לפורמט הישן** על תשעה סכומים
אמיתיים — כדי שהחלפת המימוש לא תשנה ולו פיקסל.

**הניווט** — ארבע קבוצות לפי עולם פיננסי, ובכל אחת הסדר הוא הזרימה:

| קבוצה | הסדר |
|---|---|
| משק הבית | תנועות ← תזרים |
| השקעות | תיקים ← ביצועים ← תחקור ← יומן ← מזומן בתיק |
| נכסים והתחייבויות | חסכונות ופנסיה |

לכל פריט נוספה **שורת הסבר** (״קליטה וסיווג״, ״כמה נשאר״). בלעדיה התפריט
אומר מה קיים אבל לא מה מוביל למה — וזו הייתה התלונה.

שני שינויי שם סוגרים התנגשות: `journal` מ״יומן תנועות״ ל״יומן״,
ו-`cashflow` מ״תזרים תיקים״ ל״מזומן בתיק״. המסך הזה מציג הפקדות,
דיבידנדים, עמלות ומס — זה מזומן, לא תזרים.

בדיקה חיה נועלת ש״תנועות״ ו״תזרים״ מופיעות **פעם אחת כל אחת** בתפריט,
ושהתפריט נכנס בגובה המסך אחרי שהוא גדל לשתי שורות לפריט.

## נמסר 7.9.26 — גרסה 16

`Ingest.gs` המלא כולל `bank.approve` (50,378 תווים · `cpiga1`).
אומת חי בשתי קריאות שנופלות על ולידציה ולכן אינן כותבות דבר:
שורה בלי דלי → ״חסר דלי לשורה״, ודלי שאינו אחד מהארבעה → ״דלי לא מוכר״.
טאב `Bank`: 77 שורות, **6 עדיין בלי דלי** — ממתינות לסיווג במסך.

## נמסר 7.9.26 — התוכנית החודשית (גרסה 17, `?v=29`)

טאב `Plan` + `plan.save`, מנוע `js/modules/plan.js`, מסך `js/components/plan.js`
כטאב שני בתוך תזרים (״בפועל״ / ״תוכנית״). 481 בדיקות ירוקות.

**הכרעות שקובעות אם המספר נכון — לא לשנות בלי לקרוא:**

1. **חודש טיפוסי אחד, לא שורה לחודש.** תוכנית שצריך למלא מחדש כל 30 יום
   לא תמולא, והמסך היה מציג ריק בכל ראשון לחודש.
2. **רק `הכנסה` ו`צריכה`.** `העברה` ו`הון` מזיזים כסף בלי לשנות שווי נטו,
   ומסך התזרים כבר לא סופר אותם. תוכנית שסופרת אותם מודדת משהו אחר
   מהמסך שמעליה.
3. **הבפועל מגיע מ-`FlowEngine` ולא מחישוב שני.** התוכנית רק **מפצלת** את
   אותו סכום. בדיקה נועלת: סך הפיצול = סך התזרים לאותו חודש, בשלושה
   מדדים. שברתי את הניכוי בכוונה — היא האדימה ב-4:4100 מול 4500.
4. **הסכום נשמר תמיד חיובי.** הכיוון חי בדלי.
5. **המזהה מחושב רק בשרת** (`planId_`, FNV-1a על `charCodeAt`). הלקוח מחזיר
   מזהה שקיבל או שולח שורה בלי מזהה. אין שני מימושים בשתי שפות.
6. **ביטול הוא `Active=false`, לא מחיקה** — כדי שהשוואה לחודש קודם לא
   תשתנה למפרע.

### מלכודת: מעטפה שסופרת פעמיים את ההוראה הקבועה
קטגוריה יכולה להחזיק גם שורה קבועה בשם וגם מעטפה. בלי ניכוי, אותם ₪400
מתוקצבים פעמיים. הניכוי חי בשני מקומות — `envelopes()` בהצעה ו-`compare()`
בהשוואה — ובשניהם `בפועל של מעטפה = כל הקטגוריה − השורות הקבועות שבה`.
⚠️ **הבדיקה הראשונה שכתבתי לזה לא נעלה כלום**: בפיקסצ׳ר לא הייתה קטגוריה
שיש בה גם שורה קבועה וגם מעטפה, ולכן הניכוי היה no-op. שברתי את הקוד,
הבדיקות נשארו ירוקות, והוספתי ״מנוי סופר״ בקטגוריית מזון. עכשיו השבירה
מאדימה 7 בדיקות. *בדיקה שלא ראית אותה אדומה אינה בדיקה.*

### מלכודת: ״לא הופיע החודש״ על מעטפה
`missing` נגזר מ-`rows===0`, וזה נכון לשורה בשם (תשלום שלא הגיע — בדיוק
הערך של פריט קבוע) ו**שגוי למעטפה** (לא הוצאנו בקטגוריה — חדשות טובות).
בגרסה הראשונה זה צבע ארבע מעטפות בכתום והפך את האזהרה האמיתית לרעש.
היום: `missing: p.kind === KIND.line && rows === 0`.

### מלכודת: ״בהמתנה״ אינה קטגוריה
מעטפה בשם ״בהמתנה״ הייתה מקבעת את חוסר הידיעה כתקציב לגיטימי. היא נשארת
מחוץ למעטפות ומדווחת כאזהרה למעלה.

### סכומי ההצעה — שקלים שלמים
`Math.round`, לא `round2`. ממוצע של 13,525.69 אינו מדויק יותר מ-13,526 —
הוא רק נראה כמו מדידה במקום כמו החלטה.

### חיסכון בעריכת ingest.gs בעורך
במקום להדביק 50KB, החלתי את הטלאי **בתוך העמוד** על ה-model הקיים
(`replace` עם ספירת התאמות = 1), ואימתתי אורך + `h32` על בייטי UTF-8 מול
הקובץ המקומי: 50,716 תווים · 58,578 בייטים · `mahfhd`. `plan.gs`: 5,931 ·
7,088 · `qiv6py`. הדפוס הזה עדיף על הדבקה מלאה בכל טלאי קטן.

## מצב הנתונים (7.9.26, נמדד חי על 4 חודשים)

הכנסה קבועה מזוהה: **₪30,875 לחודש** בחמש שורות.
הוצאה מסווגת: **₪10,186 לחודש**. הוצאה **לא מסווגת: ₪9,812 לחודש** —
כמעט מחצית. ⚠️ **התוכנית לא יכולה להיות נכונה עד שהיא תסווג**, וזה
המספר הראשון שהמסך אומר.

## נמסר 7.9.26 — יעדי חיסכון (גרסה 18, `?v=30`)

טאב `Goals` + `goals.save` + `goals.assign`, מנוע `js/modules/goals.js`, מסך
`js/components/goals.js` כפריט שלישי במשק הבית (תנועות → תזרים → **יעדים**).
581 בדיקות ירוקות.

**הכרעות (אושרו מול יועד):**

1. **מקור אמת אחד ליתרה.** `נחסך = Opening + סכום שורות העו״ש המתויגות`.
   ⚠️ **שווי תיק ההשקעות אינו מחובר לכאן.** יועד ביקש אינטגרציה מול התיק,
   וזו בדיוק המלכודת: העברה לתיק שמסומנת ליעד **ושווי התיק** הם אותו כסף.
   יעד שיסכם את שניהם יציג פי שניים. הפתרון לסבב הבא הוא ש**יעד שמלווה תיק
   מציג הפקדות ושווי בנפרד**, והפער ביניהם הוא התשואה — לא סכום אחד.
   העמודות `Source` ו-`LinkedPortfolio` שמורות; `Source='תיק'` **נדחה
   מפורשות בשרת** ולא מתקבל בשקט, כדי שלא ייווצר יעד שהמסך לא יודע לחשב.
2. **`Opening` היא הודאה, לא הערכה** — ״כבר יש לי ₪X מלפני שהתחלתי לסמן״.
   בלעדיה יועד היה צריך לסמן שנתיים אחורה.
3. **`Monthly` היא הקצאה, לא חישוב.** היעדים מתחרים על אותו עודף חודשי
   (`plannedSaved` מהתוכנית), והמסך **נדלק באדום** כשסך ההקצאות עולה עליו.
4. **התחזית רצה על ההקצאה, לא על הקצב שנמדד.** חודש שבו לא הפרשנו היה מותח
   את הממוצע ואת התחזית איתו. הקצב הנמדד מוצג בנפרד — הפער הוא המידע.
5. **יעד עם תאריך הופך את השאלה:** במקום ״מתי״ הוא עונה ״כמה **דרוש** בחודש״
   ומסמן חוסר מול ההקצאה.
6. **ביטול הוא `Active=false`.** שיוך תנועה הוא **הפיך** — אחרת איש לא יסמן.
7. **מכנה חסר הוא `null` ולא 0.** בלי תוכנית, ״פנוי ₪0״ היה נקרא ״אין לך כסף״
   במקום ״עוד לא אמרת כמה אתה מתכנן״.

### מלכודת: עמודה שמורה שאיש לא קורא
`GoalId` ישבה בטאב `Bank` מהיום הראשון, אבל `BankEngine.parseRows` **מעולם לא
החזירה אותה**. `GoalsEngine` קיבל `undefined` מכל שורה — כלומר ״שום דבר לא
משויך״ — **בשקט מוחלט**, בלי שגיאה ובלי אזהרה. הבדיקה תפסה את זה (24 נכשלו),
אבל רק כי היא נכתבה מול נתונים שיש בהם תיוגים. *עמודה ששמורה מראש היא חוב
שקט עד שמישהו קורא אותה בפועל.*

### מה נבחן אדום
`סילוק אשראי לא מסונן` → 3 · `pct לא נחסם ב-100` → 1 · `יעד שהושלם עדיין
צורך מהעודף` → 1 · `over לא נחסם באפס` → 3. כל אחת מהשבירות מאדימה.

### נדחה מגרסה 1 — ולמה
- **קישור יעד לתיק השקעות** — דורש את כל ספר העסקאות ומחירים חיים, הופך מסך
  מהיר לאיטי, והסמנטיקה שלו (הפקדות מול שווי) היא אפיון בפני עצמו.
- **חלוקת עודף לפי עדיפויות** (במקום הקצאה מפורשת) — נבחר במפורש נגדה:
  היא לא יכולה להראות ״כמה באמת הולך לכל אחד״ ולא יכולה להגיד ״לא״.
- היסטוריית יעד, יעדים משותפים, שיוך אוטומטי לפי כלל.
